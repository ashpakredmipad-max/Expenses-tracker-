package com.example.ui.components

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

@Composable
fun CategoryIconBadge(iconName: String, colorHex: String, size: Int) {
    CategoryIconBadge(
        iconName = iconName,
        colorHex = colorHex,
        modifier = Modifier,
        size = size
    )
}
